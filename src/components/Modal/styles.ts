import styled from '@emotion/styled';
import * as C from '@chakra-ui/react';

export const Container = styled(C.Flex)``;

export const Wrapper = styled(C.Flex)``;

export const Modal = styled(C.Modal)``;

export const ModalOverlay = styled(C.ModalOverlay)``;

export const ModalContent = styled(C.ModalContent)``;

export const ModalHeader = styled(C.ModalHeader)``;

export const ModalCloseButton = styled(C.ModalCloseButton)``;

export const ModalBody = styled(C.ModalBody)``;

export const ModalFooter = styled(C.ModalFooter)``;
